package net.laserdiamond.ventureplugin.stats.Config;

import net.laserdiamond.ventureplugin.VenturePlugin;
import net.laserdiamond.ventureplugin.util.Config.PlayerConfig;

public class BaseStatsConfig extends PlayerConfig {

    // TODO: BaseStatsConfig should be a PlayerConfig object now
    public BaseStatsConfig(VenturePlugin plugin, String fileName) {
        super(plugin, fileName);
    }
}
