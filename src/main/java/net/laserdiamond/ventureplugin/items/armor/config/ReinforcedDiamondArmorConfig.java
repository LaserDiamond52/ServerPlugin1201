package net.laserdiamond.ventureplugin.items.armor.config;

import net.laserdiamond.ventureplugin.VenturePlugin;
import net.laserdiamond.ventureplugin.util.File.ArmorConfig;

public class ReinforcedDiamondArmorConfig extends ArmorConfig {
    public ReinforcedDiamondArmorConfig(VenturePlugin plugin, String fileName) {
        super(plugin, fileName);
    }
}
